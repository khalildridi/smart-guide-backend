# smart-guide-backend

Backend API extracted from smart-guide frontend repo.

## Run

`npm install`
`npm run dev`

## Docker

Build and run:

```bash
docker compose up --build -d
```

Logs:

```bash
docker compose logs -f
```

Stop:

```bash
docker compose down
```
